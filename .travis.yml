language: python
python: "3.6"

matrix:
  include:
    - name: "Formatting check"
      before_install: pip install black
      install: skip
      script: black --check .
    - name: "Tests"
      before_install: pip install pytest
      install: pip install .
      script: pytest test
#
#
#install:
#    - pip install .
#
#script:
#    - pytest test

#before_install:
#  - pip install black
#  - pip install pytest
#
#install:
#  - pip install .
#
#script:
#  - black --check .
#  - pytest test

